# 🔄 Оновлення моделі до llama3.2

## ✅ Виконані зміни

### 1. Оновлено конфігурацію моделі

**Файл:** `config.py`

```python
# Було:
OLLAMA_MODEL = os.getenv("OLLAMA_MODEL", "llama3.1:8b")

# Стало:
OLLAMA_MODEL = os.getenv("OLLAMA_MODEL", "llama3.2")
```

### 2. Оновлено документацію

**Файл:** `README.md`

- ✅ Оновлено приклади використання
- ✅ Змінено рекомендовану модель на llama3.2
- ✅ Оновлено команди завантаження моделі

## 🚀 Як використовувати

### Варіант 1: Використання за замовчуванням

Модель `llama3.2` тепер встановлена за замовчуванням. Просто запустіть бота:

```bash
python main.py
```

### Варіант 2: Через .env файл

Якщо хочете використовувати іншу модель, створіть файл `.env`:

```env
OLLAMA_MODEL=llama3.2
```

### Варіант 3: Зміна через змінні середовища

```bash
# Windows PowerShell
$env:OLLAMA_MODEL="llama3.2"
python main.py

# Linux/Mac
export OLLAMA_MODEL=llama3.2
python main.py
```

## 📥 Завантаження моделі

Якщо у вас ще немає моделі llama3.2:

```bash
ollama pull llama3.2
```

Перевірка встановлених моделей:

```bash
ollama list
```

## 🎯 Переваги llama3.2

1. **Менші вимоги до ресурсів**
   - Потребує ~4GB RAM (порівняно з ~8GB для llama3.1:8b)
   - Швидша генерація відповідей

2. **Покращена якість**
   - Оновлена архітектура
   - Краще розуміння контексту
   - Покращена робота з українською мовою

3. **Оптимізація**
   - Краща ефективність використання пам'яті
   - Швидша обробка запитів

## ✅ Перевірка

Після оновлення перевірте:

1. **Модель встановлена:**
   ```bash
   ollama list
   ```

2. **Модель працює:**
   ```bash
   ollama run llama3.2 "Привіт"
   ```

3. **Бот використовує правильну модель:**
   - Перевірте логи при запуску бота
   - Модель має відображатися як `llama3.2`

## 📝 Примітки

- Якщо у вас вже є `.env` файл з іншою моделлю, він матиме пріоритет
- Для зміни моделі просто оновіть значення в `.env` або змінних середовища
- Всі оптимізації працюють з будь-якою моделлю OLLAMA

**Оновлення завершено! 🎉**

