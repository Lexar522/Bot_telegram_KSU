# 📤 Ручне завантаження на GitHub

Через проблеми з PowerShell, виконайте команди вручну в терміналі.

## 🚀 Швидка інструкція

### Варіант 1: Через Git Bash або CMD

1. **Відкрийте Git Bash або CMD** в папці проекту:
   ```
   D:\Різне\Bot_telegram_KSU\Bot_V1.4 – копія
   ```

2. **Виконайте команди по черзі:**

```bash
# Ініціалізація git
git init

# Додавання remote репозиторію
git remote add origin https://github.com/Lexar522/Bot_telegram_KSU.git

# Додавання файлів
git add .

# Створення commit
git commit -m "Version 1.4: Admin panel, DB optimizations, auto academic year"

# Створення гілки main
git branch -M main

# Завантаження на GitHub
git push -u origin main
```

### Варіант 2: Через GitHub Desktop

1. Відкрийте GitHub Desktop
2. File → Add Local Repository
3. Виберіть папку: `D:\Різне\Bot_telegram_KSU\Bot_V1.4 – копія`
4. Натисніть "Publish repository"
5. Виберіть репозиторій: `Lexar522/Bot_telegram_KSU`
6. Натисніть "Publish"

### Варіант 3: Через Visual Studio Code

1. Відкрийте папку проекту в VS Code
2. Відкрийте термінал (Ctrl + `)
3. Виконайте команди з Варіанту 1

## ⚠️ Якщо виникають помилки

### Помилка: "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/Lexar522/Bot_telegram_KSU.git
```

### Помилка: "failed to push some refs"
```bash
# Отримайте останні зміни
git pull origin main --allow-unrelated-histories

# Або якщо потрібно перезаписати (УВАГА!)
git push -u origin main --force
```

### Помилка авторизації
- Використайте Personal Access Token замість пароля
- Або налаштуйте SSH ключ

## ✅ Перевірка

Після завантаження перевірте:
- https://github.com/Lexar522/Bot_telegram_KSU
- Всі файли мають бути завантажені
- README.md, CHANGELOG.md, VERSION.md мають бути присутні

## 📝 Створення Release

Після завантаження коду:

1. Перейдіть на GitHub: https://github.com/Lexar522/Bot_telegram_KSU
2. Натисніть "Releases" → "Create a new release"
3. Tag: `v1.4`
4. Title: "Версія 1.4 - Адмін-панель та оптимізації"
5. Опис: скопіюйте з `RELEASE_NOTES.md`
6. Натисніть "Publish release"

---

**Примітка:** Якщо все ще виникають проблеми, використайте GitHub Desktop - це найпростіший спосіб.

