# 🔄 Інструкція з оновлення проекту в Docker

Ця інструкція допоможе оновити залежності та застосувати міграції БД для проекту, що працює в Docker.

## 📋 Перед початком

**Важливо:**
- Зробіть резервну копію БД перед оновленням
- Переконайтеся, що у вас є доступ до `.env` файлу
- Перевірте, що Docker запущений

## 🔧 Крок 1: Резервна копія бази даних

```bash
# Створіть резервну копію БД
docker exec admission_bot_db pg_dump -U postgres admission_bot > backup_$(date +%Y%m%d_%H%M%S).sql

# Або якщо використовуєте docker-compose
docker-compose exec postgres pg_dump -U postgres admission_bot > backup_$(date +%Y%m%d_%H%M%S).sql
```

**Windows PowerShell:**
```powershell
$date = Get-Date -Format "yyyyMMdd_HHmmss"
docker exec admission_bot_db pg_dump -U postgres admission_bot > "backup_$date.sql"
```

## 📦 Крок 2: Оновлення залежностей

### Варіант A: Оновлення requirements.txt

1. **Оновіть файл `requirements.txt`** (якщо потрібно):
   ```bash
   # Перевірте поточні версії
   cat requirements.txt
   ```

2. **Перебудіть Docker образ з новими залежностями:**
   ```bash
   # Зупиніть бота (БД залишається працювати)
   docker-compose stop bot
   
   # Перебудіть образ з новими залежностями
   docker-compose build --no-cache bot
   
   # Або якщо потрібно оновити тільки залежності без повної перебудови
   docker-compose build bot
   ```

### Варіант B: Оновлення залежностей в існуючому контейнері (не рекомендується)

Якщо потрібно швидко оновити залежності без перебудови:

```bash
# Увійдіть в контейнер
docker exec -it admission_bot bash

# Оновіть залежності
pip install --upgrade -r requirements.txt

# Вийдіть
exit

# Перезапустіть контейнер
docker-compose restart bot
```

**⚠️ Увага:** Цей метод не зберігає зміни після перебудови образу. Краще використовувати Варіант A.

## 🗄️ Крок 3: Застосування міграцій БД

### Автоматичні міграції

Більшість міграцій виконуються автоматично при запуску бота:

- ✅ Створення таблиць (якщо їх немає)
- ✅ Створення індексів (якщо їх немає)
- ✅ Додавання нових колонок (якщо їх немає)

**Що відбувається автоматично:**
- При запуску `main.py` викликається `db.connect()`
- `db.connect()` викликає `create_tables()`
- `create_tables()` викликає `create_indexes()`

### Ручне застосування міграцій

Якщо потрібно застосувати міграції без запуску бота:

```bash
# Запустіть Python в контейнері бота
docker exec -it admission_bot python -c "
import asyncio
from database import Database

async def migrate():
    db = Database()
    await db.connect()
    print('✅ Міграції застосовано')
    await db.disconnect()

asyncio.run(migrate())
"
```

### Перевірка індексів

Перевірте, що всі індекси створені:

```bash
# Підключіться до БД
docker exec -it admission_bot_db psql -U postgres -d admission_bot

# Перевірте індекси
\di

# Або конкретні індекси
SELECT indexname, tablename FROM pg_indexes WHERE schemaname = 'public';

# Вийдіть
\q
```

## 🧹 Крок 4: Очищення старих даних (опціонально)

Після оновлення рекомендується очистити старі дані:

```bash
# Очистити дані старші за 30 днів
docker exec -it admission_bot python main.py cleanup_db 30

# Або через docker-compose
docker-compose exec bot python main.py cleanup_db 30
```

## 🚀 Крок 5: Запуск оновленого проекту

```bash
# Запустіть всі сервіси
docker-compose up -d

# Перевірте логи
docker-compose logs -f bot

# Перевірте статус
docker-compose ps
```

## 🔍 Крок 6: Перевірка роботи

1. **Перевірте логи бота:**
   ```bash
   docker-compose logs bot | tail -50
   ```

2. **Перевірте підключення до БД:**
   ```bash
   docker-compose exec bot python -c "
   import asyncio
   from database import Database
   
   async def test():
       db = Database()
       await db.connect()
       print('✅ Підключення до БД успішне')
       await db.disconnect()
   
   asyncio.run(test())
   "
   ```

3. **Перевірте, що бот відповідає:**
   - Відкрийте Telegram
   - Надішліть `/start` боту
   - Перевірте, що бот відповідає

## 📝 Повний процес оновлення (скорочено)

```bash
# 1. Резервна копія
docker exec admission_bot_db pg_dump -U postgres admission_bot > backup.sql

# 2. Зупинити бота
docker-compose stop bot

# 3. Оновити код (якщо потрібно)
git pull origin main

# 4. Перебудувати образ
docker-compose build bot

# 5. Запустити
docker-compose up -d bot

# 6. Перевірити логи
docker-compose logs -f bot
```

## 🛠️ Вирішення проблем

### Проблема: "Requirements file not found"

**Рішення:**
```bash
# Перевірте, що requirements.txt існує
ls -la requirements.txt

# Перебудуйте образ
docker-compose build --no-cache bot
```

### Проблема: "Table already exists" або помилки міграцій

**Рішення:**
```bash
# Перевірте структуру таблиць
docker exec -it admission_bot_db psql -U postgres -d admission_bot -c "\d"

# Якщо потрібно, видаліть проблемну таблицю (ОБЕРЕЖНО!)
# docker exec -it admission_bot_db psql -U postgres -d admission_bot -c "DROP TABLE IF EXISTS table_name;"
```

### Проблема: "Index already exists"

**Рішення:**
Це нормально - індекси створюються з `IF NOT EXISTS`, тому помилки не буде. Якщо все ж таки є помилка:

```bash
# Перевірте індекси
docker exec -it admission_bot_db psql -U postgres -d admission_bot -c "\di"

# Видаліть проблемний індекс (якщо потрібно)
# docker exec -it admission_bot_db psql -U postgres -d admission_bot -c "DROP INDEX IF EXISTS index_name;"
```

### Проблема: Бот не запускається після оновлення

**Рішення:**
```bash
# 1. Перевірте логи
docker-compose logs bot

# 2. Перевірте .env файл
cat .env

# 3. Перевірте підключення до БД
docker-compose exec postgres pg_isready -U postgres

# 4. Перезапустіть всі сервіси
docker-compose down
docker-compose up -d
```

## 📚 Додаткова інформація

### Структура міграцій в проекті

Проект використовує **автоматичні міграції** через `CREATE TABLE IF NOT EXISTS` та `DO $$ ... END $$;` блоки:

1. **Таблиці:** Створюються автоматично в `database.py` → `create_tables()`
2. **Індекси:** Створюються автоматично в `database.py` → `create_indexes()`
3. **Колонки:** Додаються через `DO $$ ... END $$;` блоки (наприклад, `is_processed` в `shared_contacts`)

### Як додати нову міграцію

1. Додайте SQL в `database.py` → `create_tables()`:
   ```python
   await conn.execute("""
       DO $$ 
       BEGIN
           IF NOT EXISTS (
               SELECT 1 FROM information_schema.columns 
               WHERE table_name='table_name' AND column_name='new_column'
           ) THEN
               ALTER TABLE table_name ADD COLUMN new_column TYPE DEFAULT value;
           END IF;
       END $$;
   """)
   ```

2. Перезапустіть бота - міграція застосується автоматично.

## ✅ Чеклист оновлення

- [ ] Резервна копія БД створена
- [ ] `requirements.txt` оновлено (якщо потрібно)
- [ ] Docker образ перебудовано
- [ ] Бот запущено та перевірено
- [ ] Логи перевірено на помилки
- [ ] Функціональність протестовано
- [ ] Старі дані очищено (опціонально)

---

**Примітка:** Якщо виникають проблеми, перевірте логи та зверніться до розробника.

