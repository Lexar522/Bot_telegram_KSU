# 📤 Інструкція для завантаження на GitHub

Через проблеми з PowerShell та шляхами, що містять кирилицю, виконайте команди вручну.

## 🚀 Швидкий спосіб (рекомендовано)

Просто запустіть файл `git_push.bat` подвійним кліком або через командний рядок:

```cmd
git_push.bat
```

## 📝 Ручний спосіб

Якщо автоматичний скрипт не працює, виконайте команди вручну:

### 1. Відкрийте командний рядок (CMD) або PowerShell

**Важливо:** Використовуйте CMD, а не PowerShell, якщо є проблеми з шляхами.

### 2. Перейдіть в директорію проекту

```cmd
cd /d "D:\Різне\Bot_telegram_KSU\Bot_V1.4 – копія"
```

### 3. Видаліть старий репозиторій (якщо є)

```cmd
if exist .git rmdir /s /q .git
```

### 4. Ініціалізуйте git

```cmd
git init
```

### 5. Додайте remote репозиторій

```cmd
git remote add origin https://github.com/Lexar522/Bot_telegram_KSU.git
```

Якщо remote вже існує:
```cmd
git remote remove origin
git remote add origin https://github.com/Lexar522/Bot_telegram_KSU.git
```

### 6. Додайте файли

```cmd
git add .
```

### 7. Перевірте, що .env не додано

```cmd
git status
```

Якщо бачите `.env` в списку - перевірте `.gitignore`!

### 8. Створіть commit

```cmd
git commit -m "Version 1.4: Admin panel, DB optimizations, Docker update guide, auto academic year"
```

### 9. Створіть гілку main

```cmd
git branch -M main
```

### 10. Завантажте на GitHub

```cmd
git push -u origin main
```

**Якщо потрібна авторизація:**
- GitHub може попросити логін та пароль
- Або використайте Personal Access Token замість пароля
- Або налаштуйте SSH ключі

## 🔑 Авторизація в GitHub

### Варіант 1: Personal Access Token (рекомендовано)

1. Перейдіть: https://github.com/settings/tokens
2. Натисніть "Generate new token (classic)"
3. Виберіть права: `repo` (повний доступ до репозиторіїв)
4. Скопіюйте токен
5. При `git push` використайте токен замість пароля

### Варіант 2: GitHub CLI

```cmd
gh auth login
```

### Варіант 3: SSH ключі

1. Створіть SSH ключ: `ssh-keygen -t ed25519 -C "your_email@example.com"`
2. Додайте ключ в GitHub: https://github.com/settings/keys
3. Змініть remote на SSH:
   ```cmd
   git remote set-url origin git@github.com:Lexar522/Bot_telegram_KSU.git
   ```

## ✅ Перевірка

Після успішного завантаження:

1. Перейдіть на: https://github.com/Lexar522/Bot_telegram_KSU
2. Перевірте, що всі файли на місці
3. Перевірте, що `.env` НЕ в репозиторії!

## 🐛 Вирішення проблем

### Помилка: "fatal: not a git repository"

**Рішення:**
```cmd
git init
```

### Помилка: "remote origin already exists"

**Рішення:**
```cmd
git remote remove origin
git remote add origin https://github.com/Lexar522/Bot_telegram_KSU.git
```

### Помилка: "failed to push some refs" або "Updates were rejected" або "non-fast-forward"

**Рішення:**

**Варіант 1: Автоматичне виправлення (рекомендовано)**
```cmd
fix_push.bat
```

**Варіант 2: Вручну з merge**
```cmd
git fetch origin main
git pull origin main --allow-unrelated-histories --no-edit
git push -u origin main
```

**Варіант 3: Вручну з rebase**
```cmd
git fetch origin main
git pull --rebase origin main
git push -u origin main
```

**Варіант 4: Force push (ОБЕРЕЖНО! Перезапише все на GitHub!)**
```cmd
force_push.bat
```
Або вручну:
```cmd
git push -f origin main
```

⚠️ **Увага:** Force push видалить всі зміни на GitHub! Використовуйте тільки якщо впевнені, що локальна версія правильна.

**Якщо є конфлікти:**
1. Перевірте статус:
   ```cmd
   git status
   ```
2. Відкрийте файли з конфліктами (позначені як "both modified")
3. Вирішіть конфлікти вручну (видаліть маркери `<<<<<<<`, `=======`, `>>>>>>>`)
4. Додайте виправлені файли:
   ```cmd
   git add .
   git commit -m "Resolve merge conflicts"
   git push -u origin main
   ```

### Помилка: "Authentication failed"

**Рішення:**
- Використайте Personal Access Token замість пароля
- Або налаштуйте SSH ключі

### Помилка з AppData/Discord файлами

**Рішення:**
Перевірте `.gitignore` - там мають бути правила для `AppData/`. Якщо файли все одно додаються:

```cmd
git rm -r --cached AppData/
git commit -m "Remove AppData from git"
```

## 📋 Чеклист

- [ ] Git ініціалізовано в правильній директорії
- [ ] Remote репозиторій додано
- [ ] Файли додано (`git add .`)
- [ ] `.env` НЕ в списку файлів
- [ ] Commit створено
- [ ] Гілка `main` створена
- [ ] Файли завантажено на GitHub
- [ ] Перевірено на GitHub, що `.env` відсутній

---

**Примітка:** Якщо виникають проблеми, перевірте логи та зверніться до розробника.

