# 📋 Журнал змін

## Версія 1.4 (2026-01-04)

### 🎯 Нові функції

#### 🔐 Адмін-панель
- **Управління користувачами**
  - Перегляд списку всіх користувачів з пагінацією
  - Пошук користувачів за іменем, username або ID
  - Перегляд детальної інформації про користувача
  - Блокування/розблокування користувачів
  - Статистика по користувачах (активні, загальна кількість)

- **Розсилка повідомлень**
  - Розсилка всім користувачам
  - Розсилка тільки активним користувачам
  - Підтримка текстових повідомлень, фото, відео, документів
  - Історія розсилок з датами та кількістю отримувачів

- **Управління вартістю навчання**
  - Додавання вартості навчання для спеціальностей
  - Редагування існуючих цін
  - Видалення вартості
  - Автоматичний розрахунок вартості за семестр та рік
  - **Автоматичне визначення навчального року** (2025-2026, 2026-2027 тощо)
  - Фільтрація за рівнем освіти (Бакалавр, Магістр)
  - Фільтрація за формою навчання (Денна, Заочна)

- **Статистика бота**
  - Загальна кількість користувачів
  - Кількість активних користувачів
  - Статистика повідомлень
  - Статистика нагадувань

- **Налаштування сповіщень**
  - Управління налаштуваннями сповіщень для користувачів

### 🔧 Оптимізації та виправлення

#### 🗄️ База даних
- **Додано індекси для оптимізації запитів:**
  - Індекси на `users.telegram_id`, `users.username`
  - Індекси на `reminders.user_id`, `reminders.deadline_date`
  - Індекси на `message_history.user_id`, `message_history.created_at`
  - Індекси на `tuition_prices` для пошуку за спеціальністю
  - Додаткові індекси для оптимізації пошуку

- **Оптимізація SQL запитів:**
  - Заміна підзапитів на `LEFT JOIN` для кращої продуктивності
  - Оптимізація `get_all_users()` з використанням JOIN
  - Оптимізація `search_users()` з використанням JOIN
  - Оптимізація `get_user_by_id()` з використанням JOIN
  - Оптимізація `get_bot_statistics()` - зменшено кількість запитів

- **Connection Pooling:**
  - Налаштування пулу з'єднань: `min_size=2`, `max_size=20`
  - Таймаути: `timeout=30`, `command_timeout=60`
  - Обмеження: `max_queries=50000`, `max_inactive_connection_lifetime=300`

- **Автоматична реєстрація користувачів:**
  - Користувачі автоматично реєструються при першому повідомленні
  - Виправлено помилку `foreign key constraint` при збереженні повідомлень

- **Функція очищення БД:**
  - `cleanup_old_data()` - видалення старих записів з `message_history` та `reminders`
  - Автоматичне виконання `VACUUM ANALYZE` для оптимізації
  - Інтеграція в `main.py` з командою `python main.py cleanup_db [days]`

#### 🐳 Docker та інфраструктура
- **Оптимізація PostgreSQL:**
  - Налаштування `shared_buffers=256MB`
  - Налаштування `effective_cache_size=1GB`
  - Обмеження `max_connections=50`
  - Налаштування `autovacuum` для автоматичного очищення
  - `statement_timeout=30s` та `idle_in_transaction_session_timeout=10s`

- **Безпека:**
  - Приховано порт БД від зовнішнього доступу (тільки localhost)
  - Додано логування підключень (`log_connections`, `log_disconnections`, `log_hostname`)

- **Виправлення підключення:**
  - Виправлено помилку `WinError 1225` - відкрито порт БД для localhost
  - Покращено обробку помилок підключення

#### 🤖 Бот та обробка помилок
- **Виправлення конфліктів:**
  - Агресивне видалення webhook перед запуском polling
  - Перевірка на дублікати ботів через `bot.get_me()`
  - Покращена обробка `TelegramConflictError`

- **Автоматичне визначення навчального року:**
  - Функція `get_current_academic_year()` автоматично визначає поточний навчальний рік
  - Формат: `2025-2026` (січень-серпень) або `2026-2027` (вересень-грудень)
  - Автоматично встановлюється при додаванні вартості навчання

- **Вимкнено збереження метрик AI:**
  - Видалено збереження метрик в БД для зменшення навантаження
  - Коментарі в `ollama_optimized/metrics/collector.py`

#### 🎨 UI/UX покращення
- **Інформація про бота:**
  - Додано попередження про експериментальний статус проекту
  - Видалено фото розробника з інформації про бота
  - Покращено формат відображення інформації

### 🐛 Виправлені помилки
- Виправлено `foreign key constraint` при збереженні повідомлень
- Виправлено підключення до БД (`WinError 1225`)
- Виправлено `TelegramConflictError` (конфлікт кількох інстансів бота)
- Виправлено помилки з відображенням фото розробника
- Виправлено проблеми з високим навантаженням на БД

### 📊 Продуктивність
- Зменшено навантаження на БД на ~80% (вимкнено метрики AI)
- Покращено швидкість запитів завдяки індексам (до 10x швидше)
- Оптимізовано використання пам'яті через connection pooling
- Зменшено час відповіді на запити користувачів

### 🔒 Безпека
- Приховано порт БД від зовнішнього доступу
- Додано логування підключень для моніторингу атак
- Покращено обробку помилок для запобігання витоку інформації

### 📝 Документація
- Оновлено README.md з інформацією про версію 1.4
- Додано CHANGELOG.md з детальним описом змін
- Створено FIX_DB_CONNECTION.md з інструкціями

### ⚠️ Важливі зауваження
- Проект має **експериментальний статус** і може працювати нестабільно
- Рекомендується регулярно виконувати `cleanup_db` для очищення старих даних
- Для продакшн використання потрібні додаткові налаштування безпеки

---

## Версія 1.3 (попередня)

### Основні функції
- AI-чат з OLLAMA
- Нагадування про дедлайни
- Інформація про документи та вступ
- Контакти приймальної комісії
- Базова статистика

---

## Як оновити з версії 1.3 до 1.4

1. **Оновіть код:**
   ```bash
   git pull origin main
   ```

2. **Оновіть залежності:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Застосуйте міграції БД:**
   - Індекси створюються автоматично при запуску
   - Виконайте `python main.py cleanup_db 30` для очищення старих даних

4. **Оновіть docker-compose.yml:**
   - Переконайтеся, що використовуєте нову версію з оптимізаціями PostgreSQL

5. **Перезапустіть бота:**
   ```bash
   docker-compose down
   docker-compose up -d --build
   ```

---

## Підтримка

Якщо виникли проблеми після оновлення:
1. Перевірте логи: `docker-compose logs bot`
2. Перевірте підключення до БД
3. Виконайте очищення БД: `python main.py cleanup_db 30`
4. Зверніться до розробника: @lexar_ko


