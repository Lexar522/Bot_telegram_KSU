# 🐳 Запуск Docker контейнерів

## Проблема: БД не запущена

Якщо контейнери не запущені, виконайте:

### 1. Перевірте чи є контейнери
```bash
docker ps -a
```

### 2. Запустіть всі сервіси
```bash
docker-compose up -d
```

### 3. Або запустіть тільки БД
```bash
docker-compose up -d postgres
```

### 4. Перевірте статус
```bash
docker-compose ps
```

### 5. Перевірте логи БД
```bash
docker-compose logs postgres
```

## Якщо контейнери не створюються

### Перевірте конфігурацію
```bash
docker-compose config
```

### Перебудуйте контейнери
```bash
docker-compose down
docker-compose up -d --build
```

## Якщо є помилки

### Перевірте чи існує .env файл
```bash
# Створіть .env файл якщо його немає
# З мінімальними налаштуваннями:
BOT_TOKEN=ваш_токен
ADMIN_ID=ваш_id
DB_PASSWORD=ваш_пароль
```

### Перевірте чи є Docker
```bash
docker --version
docker-compose --version
```

## Структура docker-compose.yml

Файл містить:
- ✅ **postgres** - сервіс БД (контейнер `admission_bot_db`)
- ✅ **bot** - сервіс бота (контейнер `admission_bot`)
- ✅ **bot-network** - Docker мережа
- ✅ **postgres_data** - volume для даних БД

## Важливо

- Порт БД **приховано** від зовнішнього доступу (безпека)
- БД доступна тільки з Docker мережі
- Для доступу ззовні використовуйте SSH tunnel

