# 📋 План покращення моделі OLLAMA для Telegram-бота ХДУ

## 🎯 Мета
Покращити якість, швидкість та надійність відповідей AI-моделі OLLAMA для бота допомоги абітурієнтам ХДУ.

---

## 📊 Поточний стан (аналіз)

### ✅ Сильні сторони:
1. Детальний системний промпт з інструкціями
2. Валідація відповідей на заборонені університети
3. Регенерація при помилках (до 3 спроб)
4. Використання структурованої бази знань
5. Контекстне звуження для питань про вступ-2026

### ❌ Проблеми:
1. **Довгий системний промпт** (~150 рядків) - може перевантажувати модель
2. **Великий контекст** - вся база знань завжди передається
3. **Фіксовані параметри** - не адаптуються до типу питання
4. **Відсутність кешування** - однакові питання обробляються знову
5. **Немає few-shot examples** для складних питань
6. **Температура = 0.0** - занадто жорстко, може давати однакові відповіді
7. **Відсутність метрик** - не вимірюємо якість відповідей
8. **Немає оптимізації для різних типів питань**

---

## 🚀 План покращення (етапи)

### **ЕТАП 1: Оптимізація промпту** ⏱️ 2-3 години

#### 1.1. Розділення промпту на модулі
**Проблема:** Один великий промпт важко підтримувати та оптимізувати.

**Рішення:**
```python
# Створити модульну структуру промптів
class PromptBuilder:
    def build_system_prompt(self, question_type: str, context: dict) -> str:
        base = self._get_base_instructions()
        role = self._get_role_definition()
        constraints = self._get_constraints()
        examples = self._get_examples_for_type(question_type)
        knowledge = self._format_knowledge(context)
        
        return f"{base}\n\n{role}\n\n{constraints}\n\n{examples}\n\n{knowledge}"
```

**Переваги:**
- Легше тестувати окремі частини
- Можна змінювати приклади залежно від типу питання
- Менше токенів для простих питань

#### 1.2. Динамічне формування промпту
**Проблема:** Завжди передається весь контекст, навіть для простих питань.

**Рішення:**
```python
def _build_optimized_prompt(self, query: str, context: dict) -> str:
    # Визначаємо тип питання
    question_type = self._classify_question(query)
    
    # Звужуємо контекст
    relevant_context = self._extract_relevant_context(query, context)
    
    # Формуємо промпт залежно від типу
    if question_type == "simple_fact":
        return self._build_simple_prompt(query, relevant_context)
    elif question_type == "complex_analysis":
        return self._build_complex_prompt(query, relevant_context)
    elif question_type == "comparison":
        return self._build_comparison_prompt(query, relevant_context)
```

#### 1.3. Покращення few-shot examples
**Проблема:** Приклади загальні, не покривають всі типи питань.

**Рішення:**
```python
FEW_SHOT_EXAMPLES = {
    "tuition": [
        {
            "question": "Скільки коштує навчання на психолога?",
            "answer": "Вартість навчання на Психологію в ХДУ: Бакалавр (денна) - 3683 грн/місяць..."
        }
    ],
    "admission": [
        {
            "question": "Які документи потрібні для вступу?",
            "answer": "Для вступу до ХДУ потрібні: заява, документ про освіту..."
        }
    ],
    "faculties": [
        {
            "question": "Які є факультети?",
            "answer": "В ХДУ є 8 факультетів: Факультет української й іноземної філології..."
        }
    ]
}
```

---

### **ЕТАП 2: Оптимізація параметрів генерації** ⏱️ 1-2 години

#### 2.1. Адаптивні параметри залежно від типу питання
**Поточні параметри:**
```python
{
    "temperature": 0.0,      # Занадто жорстко
    "top_p": 0.3,            # Може бути занадто обмежено
    "num_predict": 500,      # Може бути занадто багато
    "repeat_penalty": 1.4,
    "top_k": 20
}
```

**Покращені параметри:**
```python
GENERATION_PARAMS = {
    "simple_fact": {
        "temperature": 0.1,      # Трохи креативності для варіацій
        "top_p": 0.5,
        "num_predict": 200,      # Менше для простих питань
        "repeat_penalty": 1.3,
        "top_k": 30
    },
    "complex_analysis": {
        "temperature": 0.2,      # Більше креативності для складних питань
        "top_p": 0.7,
        "num_predict": 600,      # Більше для складних питань
        "repeat_penalty": 1.5,
        "top_k": 40
    },
    "admission_2026": {
        "temperature": 0.0,      # Детерміновано для точних даних
        "top_p": 0.3,
        "num_predict": 400,
        "repeat_penalty": 1.4,
        "top_k": 20
    }
}
```

#### 2.2. Динамічний вибір параметрів
```python
def _get_generation_params(self, question_type: str, query_length: int) -> dict:
    base_params = GENERATION_PARAMS.get(question_type, GENERATION_PARAMS["simple_fact"])
    
    # Адаптуємо num_predict залежно від довжини питання
    if query_length < 20:
        base_params["num_predict"] = min(150, base_params["num_predict"])
    elif query_length > 100:
        base_params["num_predict"] = min(800, base_params["num_predict"] * 1.2)
    
    return base_params
```

---

### **ЕТАП 3: Покращення контексту** ⏱️ 2-3 години

#### 3.1. Інтелектуальне звуження контексту
**Проблема:** Завжди передається вся база знань (~5000+ токенів).

**Рішення:**
```python
class ContextOptimizer:
    def optimize_context(self, query: str, full_knowledge: dict) -> dict:
        # 1. Визначаємо ключові слова
        keywords = self._extract_keywords(query)
        
        # 2. Знаходимо релевантні секції
        relevant_sections = self._find_relevant_sections(keywords, full_knowledge)
        
        # 3. Обмежуємо розмір контексту
        optimized = self._limit_context_size(relevant_sections, max_tokens=2000)
        
        # 4. Додаємо завжди важливі секції (контакти, загальна інфо)
        optimized["core"] = {
            "university": full_knowledge["university"],
            "contacts": full_knowledge["contacts"]
        }
        
        return optimized
```

#### 3.2. Пріоритизація інформації
```python
CONTEXT_PRIORITY = {
    "high": ["contacts", "admission.year_2026", "documents"],
    "medium": ["faculties", "tuition"],
    "low": ["achievements", "international"]
}

def _prioritize_context(self, context: dict) -> dict:
    prioritized = {}
    for priority_level in ["high", "medium", "low"]:
        for key in CONTEXT_PRIORITY[priority_level]:
            if key in context:
                prioritized[key] = context[key]
    return prioritized
```

---

### **ЕТАП 4: Кешування та оптимізація** ⏱️ 3-4 години

#### 4.1. Кешування відповідей
**Проблема:** Однакові питання обробляються знову.

**Рішення:**
```python
from functools import lru_cache
import hashlib
import json

class ResponseCache:
    def __init__(self, max_size: int = 100):
        self.cache = {}
        self.max_size = max_size
    
    def _get_cache_key(self, query: str, context_hash: str) -> str:
        normalized_query = self._normalize_query(query)
        return hashlib.md5(f"{normalized_query}:{context_hash}".encode()).hexdigest()
    
    def get(self, query: str, context: dict) -> str | None:
        context_hash = hashlib.md5(json.dumps(context, sort_keys=True).encode()).hexdigest()
        key = self._get_cache_key(query, context_hash)
        return self.cache.get(key)
    
    def set(self, query: str, context: dict, response: str):
        if len(self.cache) >= self.max_size:
            # Видаляємо найстаріший запис
            oldest_key = next(iter(self.cache))
            del self.cache[oldest_key]
        
        context_hash = hashlib.md5(json.dumps(context, sort_keys=True).encode()).hexdigest()
        key = self._get_cache_key(query, context_hash)
        self.cache[key] = response
```

#### 4.2. Нормалізація питань для кешу
```python
def _normalize_query(self, query: str) -> str:
    # Приводимо до нижнього регістру
    normalized = query.lower().strip()
    
    # Видаляємо зайві пробіли
    normalized = re.sub(r'\s+', ' ', normalized)
    
    # Видаляємо пунктуацію (крім питальних знаків)
    normalized = re.sub(r'[^\w\s?]', '', normalized)
    
    # Сортуємо слова для однакових питань з різним порядком
    words = normalized.split()
    return ' '.join(sorted(words))
```

---

### **ЕТАП 5: Покращення валідації** ⏱️ 2-3 години

#### 5.1. Багаторівнева валідація
```python
class MultiLevelValidator:
    def validate(self, response: str, query: str) -> ValidationResult:
        # Рівень 1: Швидка перевірка (критичні помилки)
        quick_check = self._quick_validation(response)
        if not quick_check.is_valid:
            return quick_check
        
        # Рівень 2: Детальна перевірка
        detailed_check = self._detailed_validation(response, query)
        if not detailed_check.is_valid:
            return detailed_check
        
        # Рівень 3: Семантична перевірка
        semantic_check = self._semantic_validation(response, query)
        
        return semantic_check
```

#### 5.2. Контекстна валідація
```python
def _semantic_validation(self, response: str, query: str) -> ValidationResult:
    """Перевіряє, чи відповідь відповідає на питання"""
    query_lower = query.lower()
    response_lower = response.lower()
    
    # Перевірка наявності ключових слів з питання
    query_keywords = self._extract_keywords(query)
    found_keywords = sum(1 for kw in query_keywords if kw in response_lower)
    
    if found_keywords < len(query_keywords) * 0.3:
        return ValidationResult(
            is_valid=False,
            error_message="Відповідь не відповідає на питання"
        )
    
    return ValidationResult(is_valid=True)
```

---

### **ЕТАП 6: Метрики та моніторинг** ⏱️ 2-3 години

#### 6.1. Збір метрик
```python
class MetricsCollector:
    def __init__(self):
        self.metrics = {
            "total_requests": 0,
            "cache_hits": 0,
            "validation_failures": 0,
            "regeneration_count": 0,
            "response_times": [],
            "response_lengths": []
        }
    
    def record_request(self, query: str, response: str, 
                      response_time: float, from_cache: bool):
        self.metrics["total_requests"] += 1
        if from_cache:
            self.metrics["cache_hits"] += 1
        
        self.metrics["response_times"].append(response_time)
        self.metrics["response_lengths"].append(len(response))
        
        # Зберігаємо метрики в БД для аналізу
        self._save_metrics(query, response, response_time, from_cache)
    
    def get_statistics(self) -> dict:
        return {
            "cache_hit_rate": self.metrics["cache_hits"] / max(self.metrics["total_requests"], 1),
            "avg_response_time": sum(self.metrics["response_times"]) / max(len(self.metrics["response_times"]), 1),
            "avg_response_length": sum(self.metrics["response_lengths"]) / max(len(self.metrics["response_lengths"]), 1)
        }
```

#### 6.2. Логування для аналізу
```python
import logging

class AILogger:
    def log_generation(self, query: str, response: str, 
                      params: dict, response_time: float):
        logging.info(f"AI Generation: query={query[:50]}, "
                    f"time={response_time:.2f}s, "
                    f"params={params}")
        
        # Зберігаємо для аналізу помилок
        if len(response) < 50:
            logging.warning(f"Short response: {response}")
```

---

### **ЕТАП 7: Chain-of-Thought для складних питань** ⏱️ 3-4 години

#### 7.1. Реалізація CoT промпту
```python
def _build_cot_prompt(self, query: str, context: dict) -> str:
    return f"""Ти - помічник абітурієнта ХДУ. Відповідай на питання крок за кроком.

ПИТАННЯ: {query}

КРОК 1: Розуміння питання
- Про що питають? (спеціальності, вартість, документи, контакти)
- Яка конкретна інформація потрібна?

КРОК 2: Пошук в базі знань
- Яка секція бази знань містить відповідь?
- Які конкретні дані потрібні?

КРОК 3: Формування відповіді
- Як структурувати відповідь?
- Які конкретні дані включити?

КРОК 4: Перевірка
- Чи немає заборонених університетів?
- Чи правильна орфографія?
- Чи відповідь відповідає на питання?

ВІДПОВІДЬ:"""
```

#### 7.2. Використання CoT для складних питань
```python
def _should_use_cot(self, query: str) -> bool:
    """Визначає, чи потрібен Chain-of-Thought"""
    complex_indicators = [
        "порівняй", "в чому різниця", "як вибрати",
        "що краще", "яка різниця", "скільки різних"
    ]
    
    query_lower = query.lower()
    return any(indicator in query_lower for indicator in complex_indicators) or len(query) > 100
```

---

### **ЕТАП 8: Оптимізація для різних типів питань** ⏱️ 2-3 години

#### 8.1. Класифікація питань
```python
class QuestionClassifier:
    QUESTION_TYPES = {
        "factual": ["скільки", "які", "де", "коли", "що таке"],
        "comparison": ["порівняй", "в чому різниця", "що краще"],
        "procedural": ["як", "що потрібно", "які кроки"],
        "admission": ["вступ", "нмт", "документи", "кампанія"],
        "tuition": ["вартість", "ціна", "скільки коштує"],
        "faculties": ["факультет", "спеціальність", "напрям"]
    }
    
    def classify(self, query: str) -> str:
        query_lower = query.lower()
        
        # Перевіряємо специфічні типи
        for q_type, keywords in self.QUESTION_TYPES.items():
            if any(kw in query_lower for kw in keywords):
                return q_type
        
        # За замовчуванням - фактичне питання
        return "factual"
```

#### 8.2. Спеціалізовані обробники
```python
class SpecializedHandlers:
    def handle_factual(self, query: str, context: dict) -> str:
        # Простий пошук факту
        return self._simple_fact_search(query, context)
    
    def handle_comparison(self, query: str, context: dict) -> str:
        # Порівняння з використанням CoT
        return self._comparison_analysis(query, context)
    
    def handle_admission(self, query: str, context: dict) -> str:
        # Спеціальна обробка для питань про вступ
        return self._admission_specific(query, context)
```

---

## 📈 Метрики успіху

### Ключові показники (KPI):
1. **Точність відповідей** - % валідних відповідей (ціль: >95%)
2. **Швидкість відповіді** - середній час генерації (ціль: <3 сек)
3. **Cache hit rate** - % питань з кешу (ціль: >30%)
4. **Validation failure rate** - % невалідних відповідей (ціль: <5%)
5. **User satisfaction** - оцінки користувачів (ціль: >4.5/5)

### Метрики для моніторингу:
- Кількість регенерацій на запит
- Середня довжина відповіді
- Розподіл типів питань
- Найчастіші помилки валідації
- Топ питань без кешу

---

## 🛠️ Імплементація (порядок виконання)

### Тиждень 1: Фундамент
- [ ] День 1-2: Етап 1 (Оптимізація промпту)
- [ ] День 3: Етап 2 (Параметри генерації)
- [ ] День 4-5: Етап 3 (Контекст)

### Тиждень 2: Оптимізація
- [ ] День 1-2: Етап 4 (Кешування)
- [ ] День 3: Етап 5 (Валідація)
- [ ] День 4-5: Етап 6 (Метрики)

### Тиждень 3: Покращення
- [ ] День 1-2: Етап 7 (Chain-of-Thought)
- [ ] День 3-4: Етап 8 (Спеціалізація)
- [ ] День 5: Тестування та оптимізація

---

## 🔧 Технічні деталі

### Нова структура файлів:
```
ollama_optimized/
├── __init__.py
├── client.py              # Основний клієнт
├── prompt_builder.py      # Побудова промптів
├── context_optimizer.py   # Оптимізація контексту
├── question_classifier.py # Класифікація питань
├── cache.py              # Кешування
├── validators/
│   ├── multi_level.py    # Багаторівнева валідація
│   └── semantic.py       # Семантична валідація
├── metrics/
│   ├── collector.py      # Збір метрик
│   └── logger.py         # Логування
└── handlers/
    ├── factual.py        # Фактичні питання
    ├── comparison.py     # Порівняння
    └── admission.py      # Вступ
```

### Залежності:
```python
# Додаткові бібліотеки (якщо потрібно)
# - redis (для розподіленого кешу)
# - numpy (для обчислень метрик)
# - scikit-learn (для класифікації питань, опціонально)
```

---

## 🎯 Очікувані результати

### Покращення якості:
- ✅ Зменшення помилок валідації на 50-70%
- ✅ Покращення релевантності відповідей на 30-40%
- ✅ Зменшення згадок заборонених університетів до 0%

### Покращення продуктивності:
- ✅ Зменшення часу відповіді на 40-60% (завдяки кешу)
- ✅ Зменшення використання токенів на 30-50%
- ✅ Зменшення навантаження на OLLAMA на 40%

### Покращення користувацького досвіду:
- ✅ Більш точні та релевантні відповіді
- ✅ Швидші відповіді для типових питань
- ✅ Менше помилок та некоректних відповідей

---

## 📝 Примітки

1. **Поступова імплементація**: Краще впроваджувати поетапно з тестуванням
2. **A/B тестування**: Порівнювати нову та стару версії
3. **Моніторинг**: Відстежувати метрики після кожного етапу
4. **Відкат**: Мати можливість повернутися до попередньої версії

---

## 🔄 Наступні кроки

1. **Обговорення плану** - узгодити пріоритети
2. **Створення задач** - розбити на конкретні задачі
3. **Початок імплементації** - почати з Етапу 1
4. **Тестування** - тестувати кожен етап окремо
5. **Моніторинг** - відстежувати результати

---

**Автор:** AI Assistant  
**Дата:** 2025  
**Версія:** 1.0

