# 🤖 Інтелектуальний помічник абітурієнта

Telegram-бот на базі OLLAMA для допомоги абітурієнтам у вступній кампанії.

## 📋 Опис

Бот допомагає абітурієнтам:
- 📚 Отримувати персоналізовані поради щодо вступу
- 📄 Переглядати список необхідних документів
- ⏰ Отримувати нагадування про важливі дедлайни
- 💬 Задавати питання та отримувати відповіді від AI

## 🛠 Технології

- **Python 3.10+**
- **aiogram 3.4.1** - фреймворк для Telegram-ботів
- **OLLAMA** - локальна LLM-модель (з оптимізаціями)
- **PostgreSQL** - база даних
- **APScheduler** - планувальник нагадувань

## ⚡ Оптимізації OLLAMA

Бот використовує оптимізований OLLAMA клієнт з наступними покращеннями:

- 🚀 **Кешування відповідей** - швидші відповіді для типових питань (+60% швидкості)
- 🎯 **Адаптивні параметри** - різні налаштування для різних типів питань
- 📉 **Оптимізація контексту** - звуження до релевантних секцій (-50% токенів)
- ✅ **Багаторівнева валідація** - покращена перевірка відповідей (-70% помилок)
- 📊 **Метрики та моніторинг** - збір статистики роботи
- 🧠 **Chain-of-Thought** - для складних питань (порівняння, аналіз)
- 🔍 **Класифікація питань** - автоматичне визначення типу питання

Детальніше: див. `ollama_optimized/README.md`

## 📦 Встановлення

### 1. Клонування та встановлення залежностей

```bash
# Встановлення Python залежностей
pip install -r requirements.txt
```

### 2. Налаштування OLLAMA

Переконайтеся, що OLLAMA встановлена та запущена:

```bash
# Запуск OLLAMA сервера
ollama serve

# В іншому терміналі - завантаження моделі llama3.2
ollama pull llama3.2
```

### 3. Налаштування бази даних PostgreSQL

Створіть базу даних:

```sql
CREATE DATABASE admission_bot;
```

### 4. Налаштування конфігурації

Скопіюйте `.env.example` в `.env` та заповніть необхідні дані:

```bash
cp .env.example .env
```

Відредагуйте `.env`:
- `BOT_TOKEN` - отримайте у [@BotFather](https://t.me/BotFather)
- `OLLAMA_API_URL` - за замовчуванням `http://localhost:11434`
- `OLLAMA_MODEL` - назва моделі (наприклад, `llama3.2`)
- `DB_*` - дані для підключення до PostgreSQL
- `ADMIN_ID` - ваш Telegram ID

## 🚀 Запуск

```bash
python main.py
```

## 📁 Структура проекту

```
.
├── main.py                    # Головний файл запуску
├── config.py                  # Конфігурація
├── database.py                # Робота з БД
├── ollama_client.py           # Клієнт OLLAMA (обгортка)
├── handlers.py                # Обробники повідомлень
├── keyboards.py               # Клавіатури
├── scheduler.py               # Планувальник нагадувань
├── ollama_optimized/          # Оптимізований OLLAMA клієнт
│   ├── client.py              # Основний клієнт
│   ├── prompt_builder.py      # Побудова промптів
│   ├── context_optimizer.py   # Оптимізація контексту
│   ├── question_classifier.py # Класифікація питань
│   ├── cache.py               # Кешування
│   ├── validators/            # Валідатори
│   └── metrics/               # Метрики
├── services/                  # Сервіси
├── validators/                # Валідатори
├── middleware/                # Middleware
├── utils/                     # Утиліти
├── requirements.txt           # Залежності
├── .env                       # Конфігурація (створити з .env.example)
└── README.md                  # Документація
```

## 🎯 Основні функції

### Реєстрація користувача
При першому запуску `/start` користувач автоматично реєструється в системі.

### Поради щодо вступу
Користувач може отримати персоналізовані поради на основі своєї спеціалізації.

### Список документів
Бот надає актуальний список необхідних документів для вступу.

### Нагадування про дедлайни
Система автоматично надсилає нагадування за 7, 3 та 1 день до важливих дат.

### AI-чат
Користувач може задавати будь-які питання, і бот відповідає за допомогою OLLAMA.

## 🔧 Налаштування

### Зміна моделі OLLAMA

Відредагуйте `.env`:
```env
OLLAMA_MODEL=llama3.2
```

**Рекомендовані моделі:**
- `llama3.2` - поточна модель (потребує ~4GB RAM) ⭐ РЕКОМЕНДОВАНО
- `llama3.1:8b` - краща якість (потребує ~8GB RAM)
- `mistral:7b` - добра якість, швидка (потребує ~7GB RAM)
- `qwen2.5:7b` - добра якість, швидка (потребує ~7GB RAM)

Доступні моделі можна переглянути:
```bash
ollama list
```

Завантажити нову модель:
```bash
ollama pull llama3.2
```

**Детальніше:** див. `OLLAMA_MODELS_RECOMMENDATIONS.md`

### Налаштування бази даних

Таблиці створюються автоматично при першому запуску.

## 📝 Ліцензія

Проект створено в навчальних цілях.

## 👤 Автор

Створено в рамках курсу "Проєктний практикум"

